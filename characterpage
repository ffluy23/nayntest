window.showCharPage = function(num) {
  document.querySelectorAll('.char-page').forEach(p => 
    p.classList.remove('active')
  );

  const page = document.getElementById('charPage' + num);

  if (page) page.classList.add('active');
};
